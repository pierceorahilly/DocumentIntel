# Guide-ya AWS Deployment Configuration
# Fill in these values before running deploy script

# AWS Configuration
AWS_REGION=eu-west-1
AWS_ACCOUNT_ID=073118365801

# Project Configuration
PROJECT_NAME=GuideYa
ENVIRONMENT=prod

# S3 Configuration (use your existing bucket or create new)
S3_UPLOADS_BUCKET=bankbud
S3_LAMBDA_BUCKET=guideya-lambda

# Cognito Configuration
COGNITO_USER_POOL_NAME=GuideYaUserPool
COGNITO_APP_CLIENT_NAME=GuideYaDesktopClient

# DynamoDB Configuration
DYNAMODB_USERS_TABLE=GuideYa-Users
DYNAMODB_UPLOADS_TABLE=GuideYa-Uploads
DYNAMODB_TRANSACTIONS_TABLE=GuideYa-Transactions

# Lambda Configuration
LAMBDA_AUTH_FUNCTION=GuideYaAuthHandler
LAMBDA_UPLOAD_FUNCTION=GuideYaUploadHandler
LAMBDA_MEMORY_SIZE=512
LAMBDA_TIMEOUT=60

# API Gateway Configuration
API_GATEWAY_NAME=GuideYaAPI
API_GATEWAY_STAGE=prod

# Bedrock Configuration
BEDROCK_MODEL_ID=anthropic.claude-3-5-sonnet-20241022-v2:0

# IAM Configuration
IAM_ROLE_NAME=GuideYaLambdaExecutionRole
